'use strict';
$(function() {

    // ================================ 变量声明 ================================ //
    // ContextPath
    var sContextPath = _.CONTEXT_PATH;
    // 机具信息一览表格
    var oDataTable;
    var typeList=[ {"name":"A","Id":1}, {"name":"B","Id":2}];

    var nId = function() {
        if (location.pathname !== _.resolvePath(sContextPath, 'appmang/device')){
            var aslocatoin = location.pathname.split("/");
            // 获取 id
            return aslocatoin[aslocatoin.length - 1];
        }
    }();


    // ================================ 方法声明 ================================ //
    // JS插件渲染及画面初期事件绑定
    function initializePage() {

    	//CMD 40
		$('#40Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send40', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});

    	//CMD 40
		$('#514052Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send514052', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		//CMD 47
		$('#47Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send47', nId);

            $.get(sUrl).done(function() {

                // 显示模态窗口
//                $("#upload").modal();
                // 显示处理成功
    			$.confirm();
            });
		});
		

		
		// CMD 42 
		$('#42Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send42', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 44 
		$('#44Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send44', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 46
		$('#46Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send46', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 4ABCD
		$('#4ABCDBtn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send4ABCD', nId);

            $.get(sUrl).done(function(oData) {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 4ABC
		$('#4ABCBtn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send4ABC', nId);

            $.get(sUrl).done(function(oData) {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 4A
		$('#4ABtn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send4A', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 4B
		$('#4BBtn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send4B', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 4C
		$('#4CBtn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send4C', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 4D
		$('#4DBtn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send4D', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 4E
		$('#4EBtn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send4E', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 4F
		$('#4FBtn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send4F', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 51
		$('#51Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send51', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 52
		$('#52Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send52', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 53
		$('#53Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send53', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
    			_.route(_.resolvePath(sContextPath, 'logdownload'));
            });
            
		});
		
		// CMD 56
		$('#56Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send56', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 57
		$('#57Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send57', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 5D
		$('#5DBtn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send5D', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
		
		// CMD 57Btn
		$('#57Btn').click(function() {
			// 验证失败,则返回
           
            // 提交至后台,根据url进行区分
            var sUrl = _.resolvePath(sContextPath, 'api/application/send57', nId);

            $.get(sUrl).done(function() {
            	// 显示处理成功
    			$.confirm();
            });
		});
    	
    	
    	//返回上层
		$('#backBtn').click(function() {
			back();
		});
		
		
		

	}
   
    
    // 跳回一览画面
    function back(){
        _.route(_.resolvePath(sContextPath, 'appmang'));
    }
    
    
    // 画面初期数据取得
    function getPageData() {
		var sUrl = _.resolvePath(sContextPath, 'api/appmang/device', nId);
		$.get(sUrl).done(function(oData) {
			$('#kbsNo').val(oData.kbsNo);
			$('#ipAddress').val(oData.ipAddress);
			$('#connectPort').val(oData.connectPort);
		});
	}

    
    
    
    // 画面初期化
    function onPageLoad() {
    	
        // 画面初期事件绑定及JS插件渲染
        initializePage();
        
        // 画面初期数据取得
        getPageData();
    }

    // ================================ 方法调用 ================================ //
    // 画面初期化
    onPageLoad();
});
